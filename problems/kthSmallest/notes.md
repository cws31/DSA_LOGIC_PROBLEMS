# Notes for Kth Smallest Element in a BST

- Do an in-order traversal of the BST.
- In-order traversal of a BST visits nodes in sorted ascending order.
- Collect elements in a list, then return the `k-1`th element.
